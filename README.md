# SeePass | Плагин проверки для Minecraft

[![Version](https://img.shields.io/badge/Version-1.0.0-blue)](https://github.com/YourName/SeePass/releases)
[![Minecraft](https://img.shields.io/badge/Minecraft-1.16.5-green)](https://www.minecraft.net/)
[![Java](https://img.shields.io/badge/Java-17-orange)](https://adoptium.net/)
[![License: MIT](https://img.shields.io/badge/License-MIT-yellow.svg)](https://opensource.org/licenses/MIT)
[![Spigot](https://img.shields.io/badge/Platform-Spigot%20/%20Paper/%20Paper-red)](https://www.spigotmc.org/)

**SeePass** — это легкий, но эффективный плагин для серверов Minecraft, предназначенный для защиты от простых ботов и автоматического входа путем добавления этапа проверки для новых или всех игроков. Игрок должен доказать, что он человек, выполнив простое действие в игре.

## ✨ Возможности

*   **Гибкая система проверки:** Отправка игрока в виртуальную "камеру" или требование ввода кода для получения доступа к основному миру.
*   **Настраиваемые сообщения:** Полностью конфигурируемые сообщения, подсказки и инструкции для игрока.
*   **Умное отслеживание состояний:** Игроки, прошедшие проверку, запоминаются до следующего перезахода/перезапуска сервера.
*   **База данных или память:** Легкая система хранения данных о прошедших проверку игроках в файле (или, опционально, в MySQL).
*   **Команды администратора:** Удобное управление списком проверенных игроков.
*   **Поддержка PermissionsEx / LuckPerms:** Гибкая настройка прав для обхода или принудительной проверки.
*   **Полная локализация:** Готовность к переводу на любой язык через `config.yml`.

## 📦 Установка

1.  Убедитесь, что ваш сервер работает на **Spigot, Paper или совместимом ядре** версии **1.16.5** с **Java 17**.
2.  Скачайте последний релиз (`SeePass.jar`) со страницы [Releases](https://github.com/YourName/SeePass/releases).
3.  Поместите файл `SeePass.jar` в папку `plugins` вашего сервера.
4.  Перезагрузите (`/reload`) или перезапустите сервер.
5.  Настройте плагин под свои нужды через файл `plugins/SeePass/config.yml`.
6.  Готово! Новые игроки будут проходить проверку.

## ⚙️ Конфигурация (`config.yml`)

После первого запуска плагин создаст конфигурационный файл. Ниже пример его структуры с комментариями.

```yaml
# Настройки сообщений SeePass
messages:
  # Сообщения для игрока в процессе проверки
  welcome: "&6Добро пожаловать, &e{player}&6! Для доступа к серверу введите код &c/pass <код>&6."
  success: "&aПроверка пройдена! Добро пожаловать на сервер."
  fail: "&cНеверный код. Попробуйте еще раз. Осталось попыток: &e{attempts_left}&c."
  no-access: "&cУ вас нет доступа к основной территории до прохождения проверки."
  already-passed: "&aВы уже прошли проверку ранее."

# Настройки проверки
check:
  # Код, который нужно ввести (например, /pass 1234)
  correct-code: "1234"
  # Максимальное количество попыток (0 = бесконечно)
  max-attempts: 3
  # Куда телепортировать игрока на время проверки? (Мир, X, Y, Z, Yaw, Pitch)
  # Оставьте пустым, чтобы не телепортировать.
  check-location: "world_seePass 0.5 64 0.5 0 0"
  # Куда телепортировать игрока после успешной проверки?
  success-location: "world 0.5 64 0.5 0 0"

# Настройки сохранения данных
storage:
  # Тип хранения: "flatfile" (YAML файл) или "mysql"
  type: "flatfile"
  # Настройки MySQL (используются только если type: mysql)
  mysql:
    host: "localhost"
    port: 3306
    database: "minecraft"
    username: "root"
    password: "password"
    table-prefix: "seepass_"

# Настройки обновлений (опционально)
update-checker: true
```

## 📜 Команды и Права (Permissions)

### Команды для игроков
| Команда | Описание | Право (Permission) |
| :--- | :--- | :--- |
| `/pass <код>` | Основная команда для ввода кода проверки. | `seepass.use` (выдано по умолчанию) |

### Команды для администраторов
| Команда | Описание | Право (Permission) |
| :--- | :--- | :--- |
| `/seepass reload` | Перезагружает конфигурацию плагина (`config.yml`). | `seepass.admin.reload` |
| `/seepass add <никнейм>` | Вручную добавляет игрока в список прошедших проверку. | `seepass.admin.manage` |
| `/seepass remove <никнейм>` | Удаляет игрока из списка прошедших проверку (заставит пройти заново). | `seepass.admin.manage` |
| `/seepass list [страница]` | Показывает список всех игроков, прошедших проверку. | `seepass.admin.manage` |
| `/seepass version` | Показывает информацию о версии плагина. | `seepass.admin` |

### Ключевые права (Permissions)
*   `seepass.use` - Позволяет использовать команду `/pass`. **(Выдано всем по умолчанию)**
*   `seepass.bypass` - Игрок с этим правом автоматически пропускает проверку.
*   `seepass.admin` - Дает доступ ко всем административным командам.
*   `seepass.admin.reload` - Разрешает перезагружать конфиг.
*   `seepass.admin.manage` - Разрешает управлять списком игроков.

> **Совет:** Используйте плагин управления правами (LuckPerms, PermissionsEx) для тонкой настройки.

## 🚀 Использование

1.  **Для нового игрока:**
    *   Игрок заходит на сервер.
    *   Он автоматически телепортируется в зону проверки (если настроено) и получает инструкцию.
    *   Игрок вводит `/pass <код>` (код по умолчанию `1234`).
    *   При успехе он телепортируется на основной спавн и получает полный доступ.
    *   При неудаче (неверный код) получает сообщение об ошибке. После исчерпания попыток может быть кикнут (если вы добавите такую логику).

2.  **Для администратора:**
    *   Выдайте себе право `seepass.bypass`, чтобы не проходить проверку.
    *   Используйте `/seepass reload` после изменения `config.yml`.
    *   Используйте `/seepass add Notch`, чтобы вручную пропустить игрока.

## 🛠 Для разработчиков

### Сборка из исходного кода
```bash
# Клонируйте репозиторий
git clone https://github.com/YourName/SeePass.git
cd SeePass

# Соберите проект с помощью Maven
mvn clean package
```
Готовый файл `SeePass.jar` будет находиться в папке `target/`.

### API и интеграция
SeePass предоставляет простой API для интеграции с другими плагинами. Основной класс `SeePassAPI` можно получить через статический метод.

**Пример использования:**
```java
import dev.yourname.seepass.api.SeePassAPI;

public class MyPlugin extends JavaPlugin {
    public void checkPlayer(Player player) {
        // Проверить, прошел ли игрок проверку
        boolean hasPassed = SeePassAPI.getInstance().hasPlayerPassed(player.getUniqueId());
        
        // Вручную отметить игрока как прошедшего проверку
        SeePassAPI.getInstance().setPassed(player.getUniqueId(), true);
        
        // Получить локацию для проверки из конфига
        Location checkLoc = SeePassAPI.getInstance().getCheckLocation();
    }
}
```
Подробная документация по API доступна в [wiki](https://github.com/YourName/SeePass/wiki/API) (если вы ее создадите).

## 🤝 Вклад в проект (Contributing)

Мы приветствуем вклад в развитие SeePass! Чтобы предложить улучшение или исправить ошибку:
1.  Создайте **Fork** репозитория.
2.  Создайте новую ветку (`git checkout -b feature/AmazingFeature`).
3.  Внесите свои изменения и сделайте коммит (`git commit -m 'Add some AmazingFeature'`).
4.  Запушьте ветку (`git push origin feature/AmazingFeature`).
5.  Откройте **Pull Request**.

Пожалуйста, убедитесь, что ваш код соответствует стилю проекта и проходит все тесты.

## 📄 Лицензия

Этот проект распространяется под лицензией **MIT**. Это означает, что вы можете свободно использовать, изменять и распространять код с указанием авторства.

Подробнее см. файл [LICENSE](LICENSE).

## 🐛 Поддержка и Баги

Если вы нашли ошибку (баг) или у вас есть предложение по улучшению (feature request), пожалуйста, откройте **Issue** в [трекере GitHub](https://github.com/EminGame5392/SeePass/issues).

Перед созданием issue проверьте, не был ли подобный вопрос уже задан.

---

**Спасибо, что используете SeePass!** Надеемся, этот плагин сделает ваш сервер безопаснее и удобнее.
```
